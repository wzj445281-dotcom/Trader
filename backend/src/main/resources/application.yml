server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trader?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: "000000"
    driver-class-name: com.mysql.cj.jdbc.Driver

  #   Redis 配置
  data:
    redis:
      # 默认连接本地，Docker 部署时会被环境变量覆盖
      host: localhost
      port: 6379
      database: 0
      timeout: 5000

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

app:
  upload-dir: ./uploads

jwt:
  secret: ReplaceThisWithAStrongRandomKeyOrUseJvmProperty
  access-exp-ms: 3600000
  refresh-exp-ms: 2592000000